import Image from "next/image";

type GiftButtonProps = {
  name: string;
  price: number;
  animated?: boolean;
};

export default function GiftButton({ name, price, animated }: GiftButtonProps) {
  return (
    <div className="gift-item">
      <h3>{name}</h3>
      <p>{price} €</p>

      {animated && name === "Lotus" ? (
        <Image
          src="/gifts/lotus.gif"
          alt="Lotus animation"
          width={100}
          height={100}
        />
      ) : null}

      <button className="btn-gift">Send</button>
    </div>
  );
}
